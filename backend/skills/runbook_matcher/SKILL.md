# runbook_matcher

- 版本：1.1.0
- 所属 Agent：Ranger
- 调用条件：诊断已形成结构化根因，且风险策略允许进入方案匹配。
- 输入：根因、环境、风险级别、可用白名单 Runbook、可选参数。
- 输出：`matched` 或 `no_match`、匹配依据、参数、风险和回滚条件。
- 失败处理：低于阈值返回 `no_match`，不强行选择方案。
- 安全边界：只能从调用方提供的白名单 Runbook 中选择。
- 复用价值：匹配器与具体执行器解耦，可用于多个环境。
