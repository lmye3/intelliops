# system_metrics_collector

- 版本：1.1.0
- 所属 Agent：Detective
- 调用条件：事件需要本机 CPU、内存或磁盘的实时证据。
- 输入：目标服务、0–3600 秒时间范围、白名单指标。
- 输出：时间戳、真实数据来源、样本与逐指标错误。
- 失败处理：采集器异常时 SkillRun 标记 FAILED；单个指标不可用时写入 `errors`。
- 安全边界：只读，不执行命令，不接受任意指标名。
- 复用价值：任何需要本机资源证据的 Agent 都可使用。
