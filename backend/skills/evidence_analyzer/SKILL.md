# evidence_analyzer

- 版本：1.1.0
- 所属 Agent：Detective
- 调用条件：告警已经完成聚合并取得指标或日志证据。
- 输入：告警、指标、日志、可选反证。
- 输出：候选根因、置信度、事实证据引用、反证、缺失证据、LLM 状态。
- 失败处理：LLM 调用或结构校验失败时标记 `fallback_template`，记录错误回执；降级结果禁止自动执行 L2+。
- 安全边界：模型不能创建可执行命令，证据引用由系统根据真实输入生成。
- 复用价值：可用于监控、应用和受控沙箱事件。
